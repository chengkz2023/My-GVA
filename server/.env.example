# BoyKing Admin 后端环境变量示例
# 应用本身不读取 .env 文件——由部署方注入进程环境（如 systemd 的 Environment= 或 source 后启动）。

# 管理员初始密码：release 模式下未显式设置将拒绝启动（见 docs/production-readiness.md P0-4）；
# debug 模式允许不设置并使用默认值（同时打告警日志）。
ADMIN_INITIAL_PASSWORD=change-me-strong-password

# 可选：指定配置文件（不设置则按 gin 模式自动选择 config.debug.yaml / config.yaml 等）
# GVA_CONFIG=/path/to/config.yaml
