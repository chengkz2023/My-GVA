# BoyKing Admin

BoyKing Admin 是公司内部新项目的标准起点（脚手架仓库）。团队通过模板复制获得独立副本，在其上开发交付给外部客户的业务系统。本文件只记录本项目特有的词汇，不含实现细节。

## 定位与维护模型

**脚手架（Scaffold）**:
BoyKing Admin 仓库本身——公司新项目的标准起点。脚手架被复制使用，自身不是运行时产品。
_Avoid_: 基础框架、底座（当指这个仓库本身时）

**客户项目（Customer project）**:
从脚手架模板复制出的实际业务系统，交付给外部客户（多为网安领域、面向运营商的数据系统）。
_Avoid_: 子项目、业务系统（当指复制出的副本时）

**模板复制（Template-copy）**:
脚手架的维护模式——客户项目是脚手架的独立副本，各自演化；脚手架的修复按需以 cherry-pick 回流。
_Avoid_: 共享包分发、分叉合并

## 租户与交付

**单租户（Single-tenant）**:
一次部署只服务一个客户组织，数据模型不含租户标识。多租户是显式排除的边界；若某项目需要，须立项时以 ADR 重新决策，不允许补丁式追加租户列。

**对外交付（Customer-facing delivery）**:
产品面向外部客户交付，而非仅公司内部使用。安全能力以扩展接缝形式交付，不按开箱全量交付。

## 能力与接缝

**扩展接缝（Extension seam）**:
为横切能力预留的接口或钩子（密码策略、登录限流、审计输出、2FA/SSO 占位等）。脚手架提供默认实现或占位，由客户项目按需替换。
_Avoid_: 插件、中间件（当指这些钩子时）

**安全基线（Security baseline）**:
脚手架自带的最低安全加固集合：登录防爆破限流、密码策略、审计输出、验证码默认开启、管理员初始密码显式设置。

**审计输出（Audit output）**:
操作审计记录的统一出口。脚手架默认写入 MySQL；等保项目可替换为 syslog/ELK 等。

**字典管理（Dictionary management）**:
脚手架内置能力——集中维护枚举型键值（字典分类与字典项），供业务模块引用。
_Avoid_: 枚举表、码表

**行级数据权限（Row-level data scope）**:
一种模式——角色通过数据权限映射约束其可见的数据行范围。脚手架提供通用模式与示范，具体过滤字段由客户项目定义。
_Avoid_: 数据隔离（当指按角色过滤数据行时）

**多语言接缝（i18n seam）**:
前端接入 i18n 框架、默认中文；后端错误以稳定 code 契约暴露。存量文案的抽取推迟到有海外交付需求的项目。

## 部署与迁移约定

**同源代理（Same-origin proxy）**:
部署约定——Nginx 托管前端静态资源并将 `/api` 反向代理到后端，浏览器始终同源，不使用 CORS。
_Avoid_: 跨域部署

**系统表 AutoMigrate / 业务表 SQL 迁移（System AutoMigrate / Business SQL migration）**:
迁移分工约定——脚手架系统表（用户/角色/菜单/API/审计等）由 GORM AutoMigrate 维护；业务表一律使用版本化 SQL 迁移文件。
